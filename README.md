This branch provides support for the same code on Google Colab

Notice:
1.Select GPUs with cuda cores
2.Make sure the connected drive has enough storage for the merged model
3.Before running the code, clear the runtime storage to ensure enough storage for the original model

